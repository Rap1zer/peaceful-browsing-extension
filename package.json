{
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/chrome": "^0.0.326",
    "@types/node": "^24.0.8",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "npm run lint || true",
    "lint": "eslint . --ext .ts,.tsx || true",
    "format": "prettier --write .",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ci": "playwright test --reporter=github --retries=2"
  },
  "dependencies": {
    "playwright": "^1.53.1"
  },
  "type": "module"
}
